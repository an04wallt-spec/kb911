async function canvasToBlob(c,type,quality){return await new Promise((resolve,reject)=>c.toBlob(b=>b?resolve(b):reject(new Error('Не удалось сформировать файл')),type,quality))}
function asciiBytes(s){return new TextEncoder().encode(s)}
function concatBytes(parts){let n=0;for(const p of parts)n+=p.length;const out=new Uint8Array(n);let o=0;for(const p of parts){out.set(p,o);o+=p.length}return out}
function makePdfWithJpeg(jpegBytes,imgW,imgH,wMm,hMm){const wPt=wMm/25.4*72,hPt=hMm/25.4*72,objs=[];const content=`q\n${wPt.toFixed(3)} 0 0 ${hPt.toFixed(3)} 0 0 cm\n/Im0 Do\nQ\n`;objs[1]=asciiBytes('<< /Type /Catalog /Pages 2 0 R >>');objs[2]=asciiBytes('<< /Type /Pages /Kids [3 0 R] /Count 1 >>');objs[3]=asciiBytes(`<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${wPt.toFixed(3)} ${hPt.toFixed(3)}] /Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>`);objs[4]=concatBytes([asciiBytes(`<< /Length ${asciiBytes(content).length} >>\nstream\n`),asciiBytes(content),asciiBytes('endstream')]);objs[5]=concatBytes([asciiBytes(`<< /Type /XObject /Subtype /Image /Width ${imgW} /Height ${imgH} /ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${jpegBytes.length} >>\nstream\n`),jpegBytes,asciiBytes('\nendstream')]);const head=concatBytes([asciiBytes('%PDF-1.4\n%'),new Uint8Array([0xe2,0xe3,0xcf,0xd3]),asciiBytes('\n')]);let parts=[head],offsets=[0],pos=head.length;for(let i=1;i<=5;i++){offsets[i]=pos;const pre=asciiBytes(`${i} 0 obj\n`),post=asciiBytes('\nendobj\n');parts.push(pre,objs[i],post);pos+=pre.length+objs[i].length+post.length}const xrefPos=pos;let xref=`xref\n0 6\n0000000000 65535 f \n`;for(let i=1;i<=5;i++)xref+=String(offsets[i]).padStart(10,'0')+' 00000 n \n';xref+=`trailer\n<< /Size 6 /Root 1 0 R >>\nstartxref\n${xrefPos}\n%%EOF`;parts.push(asciiBytes(xref));return new Blob(parts,{type:'application/pdf'})}
async function chooseSaveHandle(suggestedName,mime,ext){if(!window.showSaveFilePicker)return null;return await window.showSaveFilePicker({id:'kb911-export',suggestedName,types:[{description:ext.toUpperCase()+' файл',accept:{[mime]:['.'+ext]}}]})}
function openSaveDialog(kind){saveKind=kind;const ext=kind==='jpg'?'JPEG':kind.toUpperCase();$('saveFormat').value=ext;const dpi=$('saveDpi'),vals=kind==='pdf'?[100,300,600,1200,1800,2400]:[100,300];dpi.innerHTML=vals.map(v=>`<option${v===300?' selected':''}>${v}</option>`).join('');$('saveFileName').value=`KB911_${$('paperSize').value}_${$('orientation').value}.${kind==='jpg'?'jpg':kind}`;dpi.value='300';const p=$('saveDialog');p.classList.add('open');p.setAttribute('aria-hidden','false');p.style.left=Math.max(10,(innerWidth-p.offsetWidth)/2)+'px';p.style.top=Math.max(60,(innerHeight-p.offsetHeight)/2)+'px'}
function closeSaveDialog(){$('saveDialog').classList.remove('open');$('saveDialog').setAttribute('aria-hidden','true')}
async function performSave(){const dpi=+$('saveDpi').value||300;let ext=saveKind==='jpg'?'jpg':saveKind,mime=saveKind==='png'?'image/png':saveKind==='jpg'?'image/jpeg':'application/pdf';let name=$('saveFileName').value.trim()||`KB911_${$('paperSize').value}.${ext}`;if(!name.toLowerCase().endsWith('.'+ext))name+='.'+ext;let handle=null;try{handle=await chooseSaveHandle(name,mime,ext)}catch(e){if(e?.name==='AbortError'){setStatus('Сохранение отменено');return}console.error(e)}closeSaveDialog();document.body.classList.add('exporting');setStatus('Формирую файл…');try{const c=await renderSheetCanvas(dpi);let blob;if(saveKind==='png')blob=await canvasToBlob(c,'image/png');else if(saveKind==='jpg')blob=await canvasToBlob(c,'image/jpeg',.95);else{const jpg=await canvasToBlob(c,'image/jpeg',.96),bytes=new Uint8Array(await jpg.arrayBuffer());blob=makePdfWithJpeg(bytes,c.width,c.height,page.w,page.h)}if(handle){const writable=await handle.createWritable();await writable.write(blob);await writable.close();setStatus('Сохранено: '+(handle.name||name))}else{downloadBlob(blob,name);setStatus('Файл сохранён: '+name)}}catch(e){console.error(e);alert('Не удалось сохранить файл: '+(e?.message||e));setStatus('Ошибка сохранения')}finally{document.body.classList.remove('exporting')}}
$('exportPng').onclick=()=>openSaveDialog('png');$('exportJpg').onclick=()=>openSaveDialog('jpg');$('exportPdf').onclick=()=>openSaveDialog('pdf');$('saveCancel').onclick=closeSaveDialog;$('saveConfirm').onclick=performSave;$('saveDialog').addEventListener('pointerdown',e=>e.stopPropagation());
$('frameEnabled').onchange=()=>{frameState.enabled=$('frameEnabled').checked;renderFrame()};$('frameCompanyEnabled').onchange=()=>{frameState.company=$('frameCompanyEnabled').checked;renderFrame()};$('frameLogoEnabled').onchange=()=>{frameState.logoEnabled=$('frameLogoEnabled').checked;renderFrame()};$('frameCompanyName').oninput=()=>{frameState.name=$('frameCompanyName').value;renderFrame()};$('frameLogoBtn').onclick=()=>$('frameLogoFile').click();$('frameLogoFile').onchange=e=>{const f=e.target.files?.[0];if(!f)return;const r=new FileReader();r.onload=()=>{frameState.logo=r.result;renderFrame()};r.readAsDataURL(f);e.target.value=''};$('frameLogoClear').onclick=()=>{frameState.logo=null;renderFrame()};
setPage();
})();
</script>
</body></html>

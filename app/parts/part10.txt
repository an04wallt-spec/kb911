$('imageOpacity').oninput=()=>{if(selected?.dataset.type==='image')selected.setAttribute('opacity',$('imageOpacity').value)};
$('imageW').oninput=()=>{if(selected?.dataset.type!=='image')return;const r=imageRect(selected),nw=Math.max(1,+$('imageW').value||1),ratio=+selected.dataset.ratio||r.w/r.h;selected.setAttribute('width',nw);selected.setAttribute('height',nw/ratio);showProps();drawSelection()};
$('imageH').oninput=()=>{if(selected?.dataset.type!=='image')return;const r=imageRect(selected),nh=Math.max(1,+$('imageH').value||1),ratio=+selected.dataset.ratio||r.w/r.h;selected.setAttribute('height',nh);selected.setAttribute('width',nh*ratio);showProps();drawSelection()};
$('btnImage').onclick=()=>$('fileImage').click();$('fileImage').onchange=e=>{[...e.target.files].forEach(loadImage);e.target.value=''};
$('centerImage').onclick=()=>{const i=selected?.dataset.type==='image'?selected:paper.querySelector('image[data-type=image]');if(!i){setStatus('Сначала загрузите картинку');return}rememberImageGeometry(i);const r=imageRect(i);i.setAttribute('x',(page.w-r.w)/2);i.setAttribute('y',(page.h-r.h)/2);if(selected===i)drawSelection();setStatus('Картинка центрирована. ↶ — вернуть обратно')};
$('undoImage').onclick=()=>{if(!imageUndo){setStatus('Нет предыдущего положения картинки');return}const i=[...paper.querySelectorAll('image[data-type=image]')].find(n=>n.dataset.id===imageUndo.id);if(!i){imageUndo=null;setStatus('Картинка для возврата не найдена');return}const cur=imageRect(i),old={...imageUndo};imageUndo={id:i.dataset.id,x:cur.x,y:cur.y,w:cur.w,h:cur.h};i.setAttribute('x',old.x);i.setAttribute('y',old.y);i.setAttribute('width',old.w);i.setAttribute('height',old.h);if(selected===i)drawSelection();setStatus('Предыдущее положение картинки восстановлено')};
$('paperSize').onchange=setPage;$('orientation').onchange=setPage;$('zoomIn').onclick=()=>{const r=viewport.getBoundingClientRect();zoomAtClientPoint(r.left+r.width/2,r.top+r.height/2,zoom*1.12)};$('zoomOut').onclick=()=>{const r=viewport.getBoundingClientRect();zoomAtClientPoint(r.left+r.width/2,r.top+r.height/2,zoom/1.12)};$('zoomReset').onclick=()=>{const r=viewport.getBoundingClientRect();zoomAtClientPoint(r.left+r.width/2,r.top+r.height/2,1)};
function measureFrameTextMm(text,fs=4){const c=document.createElement('canvas'),ctx=c.getContext('2d'),pxPerMm=96/25.4;ctx.font=`700 ${fs*pxPerMm}px Bahnschrift`;return ctx.measureText(String(text||'')).width/pxPerMm}
function frameLogoClipPath(x,y,w,h,r){return `M ${x} ${y} L ${x+w-r} ${y} Q ${x+w} ${y} ${x+w} ${y+r} L ${x+w} ${y+h} L ${x+r} ${y+h} Q ${x} ${y+h} ${x} ${y+h-r} L ${x} ${y} Z`}
function renderFrame(){paper.querySelector('[data-type="frame"]')?.remove();if(!frameState.enabled)return;const g=el('g',{'data-type':'frame','pointer-events':'none'});const inset=5,sw=.3,rad=2;const right=page.w-inset,bottom=page.h-inset;const outer=el('rect',{x:inset,y:inset,width:Math.max(0,page.w-inset*2),height:Math.max(0,page.h-inset*2),rx:rad,ry:rad,fill:'none',stroke:'#111','stroke-width':sw});
const showCompany=!!frameState.company,showLogo=!!frameState.logoEnabled;
if(showLogo){const ls=18,x=right-ls,y=inset,r=rad;if(frameState.logo){const clipId=`frameLogoClip-${uid++}`,cp=el('clipPath',{id:clipId});cp.appendChild(el('path',{d:frameLogoClipPath(x,y,ls,ls,r)}));g.appendChild(cp);g.appendChild(el('image',{href:frameState.logo,x,y,width:ls,height:ls,preserveAspectRatio:'none','clip-path':`url(#${clipId})`,'data-frame-logo':'1','data-clip-r':r}))}const d=`M ${x} ${y} L ${x} ${y+ls-r} Q ${x} ${y+ls} ${x+r} ${y+ls} L ${right} ${y+ls}`;g.appendChild(el('path',{d,fill:'none',stroke:'#111','stroke-width':sw,'stroke-linecap':'round','stroke-linejoin':'round'}))}
if(showCompany){const fs=4,padX=3,padY=1.15,label=frameState.name||'Название компании',fh=fs+padY*2,nameW=Math.max(18,measureFrameTextMm(label,fs)+padX*2),x=right-nameW,y=bottom-fh,r=rad;const d=`M ${x} ${bottom} L ${x} ${y+r} Q ${x} ${y} ${x+r} ${y} L ${right} ${y}`;g.appendChild(el('path',{d,fill:'none',stroke:'#111','stroke-width':sw,'stroke-linecap':'round','stroke-linejoin':'round'}));const t=el('text',{x:x+nameW/2,y:y+fh/2,'text-anchor':'middle','dominant-baseline':'central','font-family':'Bahnschrift','font-size':fs,'font-weight':'700',fill:'#111'});t.textContent=label;g.appendChild(t)}
g.appendChild(outer);paper.appendChild(g)}
function drawRoundedRectStroke(ctx,n){const x=+n.getAttribute('x'),y=+n.getAttribute('y'),w=+n.getAttribute('width'),h=+n.getAttribute('height'),r=+(n.getAttribute('rx')||0);ctx.save();ctx.strokeStyle=n.getAttribute('stroke')||'#111';ctx.lineWidth=+(n.getAttribute('stroke-width')||.5);roundedRectPath(ctx,x,y,w,h,r);ctx.stroke();ctx.restore()}
function drawFrameObject(ctx,g){for(const n of g.children){const tag=n.tagName.toLowerCase();if(tag==='rect')drawRoundedRectStroke(ctx,n);else if(tag==='line')drawSvgLine(ctx,n);else if(tag==='path')drawSvgPath(ctx,n);else if(tag==='text')drawSvgText(ctx,n)}}
async function drawFrameLogoImage(ctx,n){const href=n.getAttribute('href')||n.getAttributeNS('http://www.w3.org/1999/xlink','href');if(!href)return;const im=await loadCanvasImage(href),x=+n.getAttribute('x'),y=+n.getAttribute('y'),w=+n.getAttribute('width'),h=+n.getAttribute('height'),r=+(n.dataset.clipR||2);ctx.save();const p=new Path2D(frameLogoClipPath(x,y,w,h,r));ctx.clip(p);ctx.drawImage(im,x,y,w,h);ctx.restore()}

function exportDpi(){const n=$('paperSize').value;return n==='A0'?120:n==='A1'?150:n==='A2'?200:300}
function commitEditorsForExport(){const ta=document.getElementById('liveTextEditor');if(ta)ta.blur();closeDimPopup();closeTextPopup();closeImageMenu();clearSelection()}

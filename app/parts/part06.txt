value||'').split('\n')){if(raw===''){out.push('');continue}let line='';for(const word of raw.split(/\s+/)){const test=line?line+' '+word:word;if(ctx.measureText(test).width<=maxW*scale||!line)line=test;else{out.push(line);line=word}}if(line)out.push(line)}return out}
function createTextBox(x,y,w,h){const d=builtText,g=el('g',{'data-type':'text','data-id':uid++,'data-x':x,'data-y':y,'data-w':Math.max(12,w),'data-h':Math.max(8,h),'data-font':d.font,'data-font-size':d.fontSize,'data-text-color':d.textColor,'data-bg':d.bg,'data-bg-opacity':d.bgOpacity,'data-bold':d.bold?'1':'0','data-angle':d.angle||'0','data-align':d.align||'center','data-value':''});appendContent(g);renderText(g);select(g);setTool('text-edit',true);setStatus('Введите текст. Для фиксации нажмите OK, Esc или Ctrl+Enter');setTimeout(()=>editText(g),0)}
function renderText(g,value=null){if(value!==null)g.dataset.value=value;while(g.firstChild)g.removeChild(g.firstChild);const x=+g.dataset.x,y=+g.dataset.y,w=+g.dataset.w,h=+g.dataset.h,fs=+g.dataset.fontSize||4,font=g.dataset.font||'Bahnschrift',a=+g.dataset.angle||0,align=g.dataset.align||'center',cx=x+w/2,cy=y+h/2;g.setAttribute('transform',`rotate(${a} ${cx} ${cy})`);g.appendChild(el('rect',{x,y,width:w,height:h,rx:2.5,ry:2.5,fill:g.dataset.bg||'#fff','fill-opacity':g.dataset.bgOpacity||0,stroke:'none'}));const clipId=`textclip-${g.dataset.id}`,cp=el('clipPath',{id:clipId});cp.appendChild(el('rect',{x:x+1,y:y+1,width:Math.max(0,w-2),height:Math.max(0,h-2),rx:2,ry:2}));g.appendChild(cp);const tx=align==='left'?x+2:align==='right'?x+w-2:x+w/2,anchor=align==='left'?'start':align==='right'?'end':'middle',lines=wrapTextLines(g.dataset.value||'',font,fs,Math.max(2,w-4),g.dataset.bold==='1'),lh=fs*1.22,totalH=lines.length?fs+(lines.length-1)*lh:fs,firstY=cy-totalH/2+fs/2;const t=el('text',{x:tx,y:firstY,'text-anchor':anchor,'dominant-baseline':'central','font-family':font,'font-size':fs,'font-weight':g.dataset.bold==='1'?'700':'400',fill:g.dataset.textColor||'#111',class:'text-hit hover-movable','clip-path':`url(#${clipId})`});lines.forEach((line,i)=>{const sp=el('tspan',{x:tx,dy:i===0?0:lh});sp.textContent=line;t.appendChild(sp)});g.appendChild(t)}
function hexToRgba(hex,a){let h=(hex||'#fff').replace('#','');if(h.length===3)h=h.split('').map(c=>c+c).join('');const n=parseInt(h,16);return`rgba(${(n>>16)&255},${(n>>8)&255},${n&255},${a})`}
function updateLiveEditorPadding(ta,g){const fs=+g.dataset.fontSize||4,pr=paper.getBoundingClientRect(),fsPx=fs/page.w*pr.width,wMm=+g.dataset.w||60,lines=wrapTextLines(ta.value||'',g.dataset.font||'Bahnschrift',fs,Math.max(2,wMm-4),g.dataset.bold==='1'),lhPx=fsPx*1.22,total=Math.max(fsPx,fsPx+(Math.max(1,lines.length)-1)*lhPx),pad=Math.max(3,(ta.clientHeight-total)/2);ta.style.paddingTop=pad+'px';ta.style.paddingBottom=pad+'px'}
function editText(g){if(!g||g.dataset.type!=='text')return;document.getElementById('liveTextEditor')?.blur();select(g);setStatus('Введите текст. Для фиксации нажмите OK, Esc или Ctrl+Enter');const pr=paper.getBoundingClientRect(),x=+g.dataset.x,y=+g.dataset.y,w=+g.dataset.w,h=+g.dataset.h,fs=+g.dataset.fontSize||4;const ta=document.createElement('textarea');ta.id='liveTextEditor';ta.value=g.dataset.value||'';ta.style.left=(pr.left+x/page.w*pr.width)+'px';ta.style.top=(pr.top+y/page.h*pr.height)+'px';ta.style.width=(w/page.w*pr.width)+'px';ta.style.height=(h/page.h*pr.height)+'px';ta.style.fontFamily=g.dataset.font||'Bahnschrift';ta.style.fontWeight=g.dataset.bold==='1'?'700':'400';ta.style.fontSize=(fs/page.w*pr.width)+'px';ta.style.lineHeight='1.22';ta.style.textAlign=g.dataset.align||'center';ta.style.color=g.dataset.textColor||'#111';ta.style.background=hexToRgba(g.dataset.bg||'#fff',+(g.dataset.bgOpacity||0));ta.style.transformOrigin='center center';ta.style.transform=`rotate(${+g.dataset.angle||0}deg)`;g.style.visibility='hidden';ta.addEventListener('input',()=>{g.dataset.value=ta.value;$('textValue').value=ta.value;updateLiveEditorPadding(ta,g)});ta.addEventListener('keydown',e=>{if(e.key==='Escape'||(e.key==='Enter'&&(e.ctrlKey||e.metaKey))){e.preventDefault();ta.blur()}});ta.addEventListener('blur',()=>{g.dataset.value=ta.value;g.style.visibility='';renderText(g);ta.remove();select(g);setStatus('Надпись активна. При необходимости подправьте параметры и нажмите OK')},{once:true});document.body.appendChild(ta);requestAnimationFrame(()=>updateLiveEditorPadding(ta,g));ta.focus()}
function openDimPopup(){const p=$('dimPopup');p.classList.add('open');p.setAttribute('aria-hidden','false');if(!p.style.left){p.style.left=Math.max(190,(innerWidth-338)/2)+'px';p.style.top=Math.max(70,(innerHeight-320)/2)+'px'}}
function closeDimPopup(){const p=$('dimPopup');p.classList.remove('open');p.setAttribute('aria-hidden','true')}
function openTextPopup(){const p=$('textPopup');p.classList.add('open');p.setAttribute('aria-hidden','false');if(!p.style.left){p.style.left=Math.max(210,(innerWidth-338)/2+22)+'px';p.style.top=Math.max(86,(innerHeight-340)/2+18)+'px'}}
function closeTextPopup(){const p=$('textPopup');p.classList.remove('open');p.setAttribute('aria-hidden','true')}
function syncDimProps(){const g=selected;if(!g||g.dataset.type!=='dimension')return;$('dimPrefix').value=g.dataset.prefix||'';$('dimText').value=g.dataset.value||'';$('dimSuffix').value=g.dataset.suffix||'';$('arrowStyle').value=g.dataset.arrow||'slim';$('arrowAng
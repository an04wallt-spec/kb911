le').value=g.dataset.arrowAngle||10;$('arrowSize').value=g.dataset.arrowSize||5;$('tailSize').value=g.dataset.tailSize||7;$('lineWidth').value=g.dataset.lineWidth||.4;$('lineColor').value=g.dataset.lineColor||'#111111';$('fontFamily').value=g.dataset.font||'Bahnschrift';$('fontSize').value=g.dataset.fontSize||4;$('textColor').value=g.dataset.textColor||'#111111';$('fontBold').checked=g.dataset.bold==='1';$('fontItalic').checked=g.dataset.italic==='1'}
function captureDimDefaults(g){dimDefaults={prefix:g.dataset.prefix||'',suffix:g.dataset.suffix||'',arrow:g.dataset.arrow||'slim',arrowSize:g.dataset.arrowSize||'5',arrowAngle:g.dataset.arrowAngle||'10',tailSize:g.dataset.tailSize||'7',lineWidth:g.dataset.lineWidth||'0.40',lineColor:g.dataset.lineColor||'#111111',font:g.dataset.font||'Bahnschrift',fontSize:g.dataset.fontSize||'4',textColor:g.dataset.textColor||'#111111',bold:g.dataset.bold==='1',italic:g.dataset.italic==='1'};saveStore('mm.dimDefaults.v2',dimDefaults)}
function syncTextProps(){const g=selected;if(!g)return;$('textValue').value=g.dataset.value||'';$('textBoxW').value=+g.dataset.w||60;$('textBoxH').value=+g.dataset.h||18;$('textFont').value=g.dataset.font||'Bahnschrift';$('textSize').value=g.dataset.fontSize||4;$('textBold').checked=g.dataset.bold==='1';$('textAngle').value=g.dataset.angle||0;$('textAlign').value=g.dataset.align||'center';$('textFill').value=g.dataset.textColor||'#111111';$('textBg').value=g.dataset.bg||'#ffffff';$('textBgOpacity').value=g.dataset.bgOpacity||0}
function captureTextDefaults(g){return}
function cloneDimFromData(d){const g=el('g',{'data-type':'dimension','data-id':uid++,'data-p1':d.p1,'data-p2':d.p2});for(const[k,v]of Object.entries(d)){if(!['type','id','p1','p2'].includes(k))g.dataset[k]=v}return g}
function cloneTextFromData(d){const g=el('g',{'data-type':'text','data-id':uid++,'data-x':d.x,'data-y':d.y,'data-w':d.w,'data-h':d.h});for(const[k,v]of Object.entries(d)){if(!['type','id','x','y','w','h'].includes(k))g.dataset[k]=v}return g}
function startPasteMode(){if(!clipboardItem)return;cancelPasteMode();pasteDraft={type:clipboardItem.type,data:{...clipboardItem.data},anchor:null,ghost:null};paper.style.cursor='copy';const start={x:page.w/2,y:page.h/2};updatePasteGhost(start);setStatus((pasteDraft.type==='dimension'?'Размер':'Надпись')+' в режиме вставки — переместите мышь и щёлкните в нужном месте')}
function cancelPasteMode(){if(pasteDraft?.ghost)pasteDraft.ghost.remove();pasteDraft=null;paper.style.cursor=''}
function updatePasteGhost(p){if(!pasteDraft)return;const d=pasteDraft.data;if(pasteDraft.type==='dimension'){const p1=parsePt(d.p1),p2=parsePt(d.p2);const cx=(p1.x+p2.x)/2,cy=(p1.y+p2.y)/2;const dx=p.x-cx,dy=p.y-cy;d.p1=`${p1.x+dx},${p1.y+dy}`;d.p2=`${p2.x+dx},${p2.y+dy}`;if(!pasteDraft.ghost){pasteDraft.ghost=cloneDimFromData(d);pasteDraft.ghost.style.opacity='.45';pasteDraft.ghost.style.pointerEvents='none';paper.appendChild(pasteDraft.ghost)}else{pasteDraft.ghost.dataset.p1=d.p1;pasteDraft.ghost.dataset.p2=d.p2}renderDim(pasteDraft.ghost)}else if(pasteDraft.type==='text'){const x=p.x-(+d.w||60)/2,y=p.y-(+d.h||18)/2;d.x=String(x);d.y=String(y);if(!pasteDraft.ghost){pasteDraft.ghost=cloneTextFromData(d);pasteDraft.ghost.style.opacity='.45';pasteDraft.ghost.style.pointerEvents='none';paper.appendChild(pasteDraft.ghost)}else{pasteDraft.ghost.dataset.x=d.x;pasteDraft.ghost.dataset.y=d.y}renderText(pasteDraft.ghost)}}
function placePaste(){if(!pasteDraft)return;let g;if(pasteDraft.type==='dimension'){g=cloneDimFromData(pasteDraft.data);appendContent(g);renderDim(g);select(g);setTool('dimension-edit',true)}else{g=cloneTextFromData(pasteDraft.data);appendContent(g);renderText(g);select(g);setTool('text-edit',true)}lastEditable=g;if(g.dataset.type==='dimension')captureDimDefaults(g); cancelPasteMode()}
[['dimPrefix','prefix'],['dimText','value'],['dimSuffix','suffix'],['arrowStyle','arrow'],['arrowAngle','arrowAngle'],['arrowSize','arrowSize'],['tailSize','tailSize'],['lineWidth','lineWidth'],['lineColor','lineColor'],['fontFamily','font'],['fontSize','fontSize'],['textColor','textColor']].forEach(([id,key])=>$(id).addEventListener('input',()=>{if(selected?.dataset.type!=='dimension')return;selected.dataset[key]=$(id).value;renderDim(selected);drawSelection();captureDimDefaults(selected)}));
[['fontBold','bold'],['fontItalic','italic']].forEach(([id,key])=>$(id).addEventListener('change',()=>{if(selected?.dataset.type!=='dimension')return;selected.dataset[key]=$(id).checked?'1':'0';renderDim(selected);drawSelection();captureDimDefaults(selected)}));
[['textValue','value'],['textBoxW','w'],['textBoxH','h'],['textFont','font'],['textSize','fontSize'],['textAngle','angle'],['textAlign','align'],['textFill','textColor'],['textBg','bg'],['textBgOpacity','bgOpacity']].forEach(([id,key])=>$(id).addEventListener('input',()=>{if(selected?.dataset.type!=='text')return;selected.dataset[key]=$(id).value;renderText(selected);drawSelection()}));
$('textBold').addEventListener('change',()=>{if(selected?.dataset.type!=='text')return;selected.dataset.bold=$('textBold').checked?'1':'0';renderText(selected);drawSelection()});
function findOwner(id,type){return paper.querySelector(`[data-type="${type}"][data-id="${id}"]`)}
function closeImageMenu(){$('imageContextMenu').classList.remove('open');$('imageContextMenu').setAttribute('aria-hidden','true')}
function op
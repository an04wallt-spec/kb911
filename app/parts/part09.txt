 if(imageResize){const d=imageResize,dx=p.x-d.start.x,dy=p.y-d.start.y,ratio=+d.obj.dataset.ratio||d.w/d.h;const sx=d.corner.includes('e')?(d.w+dx)/d.w:(d.w-dx)/d.w,sy=d.corner.includes('s')?(d.h+dy)/d.h:(d.h-dy)/d.h;let scale=Math.abs(sx-1)>=Math.abs(sy-1)?sx:sy;scale=Math.max(.03,scale);const w=Math.max(3,d.w*scale),h=Math.max(3,d.h*scale);let x=d.x,y=d.y;if(d.corner.includes('w'))x=d.x+d.w-w;if(d.corner.includes('n'))y=d.y+d.h-h;d.obj.setAttribute('x',x);d.obj.setAttribute('y',y);d.obj.setAttribute('width',w);d.obj.setAttribute('height',h);showProps();drawSelection();return}
 if(rotateDrag){const a=Math.atan2(p.y-rotateDrag.cy,p.x-rotateDrag.cx)*180/Math.PI,na=rotateDrag.startAngle+(a-rotateDrag.startPointer);rotateDrag.obj.dataset.angle=((na+180)%360+360)%360-180;$('textAngle').value=(+rotateDrag.obj.dataset.angle).toFixed(1);renderText(rotateDrag.obj);drawSelection();return}
 if(tailDrag){const g=tailDrag.obj,{p1,p2,nx,ny}=dimGeom(g),d1=Math.abs((p.x-p1.x)*nx+(p.y-p1.y)*ny),d2=Math.abs((p.x-p2.x)*nx+(p.y-p2.y)*ny);g.dataset.tailSize=Math.max(1,Math.min(80,2*Math.min(d1,d2)));$('tailSize').value=(+g.dataset.tailSize).toFixed(1);renderDim(g);drawSelection();captureDimDefaults(g);return}
 if(endpointDrag){endpointDrag.obj.dataset[endpointDrag.end]=`${p.x+endpointDrag.offX},${p.y+endpointDrag.offY}`;renderDim(endpointDrag.obj);drawSelection();return}
 if(textResize){const dx=p.x-textResize.start.x,dy=p.y-textResize.start.y,g=textResize.obj;g.dataset.w=Math.max(5,textResize.w+dx);g.dataset.h=Math.max(5,textResize.h+dy);renderText(g);showProps();drawSelection();return}
 if(!drag)return;const dx=p.x-drag.start.x,dy=p.y-drag.start.y,g=drag.obj;if(g.dataset.type==='dimension'){g.dataset.p1=`${drag.p1.x+dx},${drag.p1.y+dy}`;g.dataset.p2=`${drag.p2.x+dx},${drag.p2.y+dy}`;renderDim(g)}else if(g.dataset.type==='text'){g.dataset.x=drag.x+dx;g.dataset.y=drag.y+dy;renderText(g)}else if(g.dataset.type==='image'){g.setAttribute('x',drag.x+dx);g.setAttribute('y',drag.y+dy)}drawSelection()
});
window.addEventListener('pointerup',e=>{if(dimDraft&&tool==='dimension'){const p2=pt(e),p1=dimDraft.p1;dimDraft=null;dimPreview?.remove();dimPreview=null;if(Math.hypot(p2.x-p1.x,p2.y-p1.y)>2)createDim(p1,p2)}if(textDraft&&tool==='text'){const p2=pt(e),p1=textDraft.p1;textDraft=null;textPreview?.remove();textPreview=null;const x=Math.min(p1.x,p2.x),y=Math.min(p1.y,p2.y),w=Math.abs(p2.x-p1.x),h=Math.abs(p2.y-p1.y);createTextBox(x,y,w>4?w:60,h>4?h:18)}drag=null;textResize=null;imageResize=null;endpointDrag=null;tailDrag=null;rotateDrag=null});
document.addEventListener('keydown',e=>{
 const ctrl=e.ctrlKey||e.metaKey;
 const copyKey=ctrl&&(e.code==='KeyC'||e.key?.toLowerCase()==='c'||e.key==='с'||e.key==='С');
 const pasteKey=ctrl&&(e.code==='KeyV'||e.key?.toLowerCase()==='v'||e.key==='м'||e.key==='М');
 if(copyKey){
   const source=(selected&&['dimension','text'].includes(selected.dataset?.type))?selected:((hoverDim&&hoverDim.dataset?.type==='dimension')?hoverDim:((lastEditable&&lastEditable.isConnected)?lastEditable:null));
   if(source){e.preventDefault();e.stopPropagation();clipboardItem={type:source.dataset.type,data:{...source.dataset}};lastEditable=source;setStatus((source.dataset.type==='dimension'?'Размер':'Надпись')+' скопирован(а). Ctrl+V — вставить');return}
 }
 if(pasteKey&&clipboardItem){e.preventDefault();e.stopPropagation();closeDimPopup();closeTextPopup();clearUI();selected=null;startPasteMode();return}
 const inField=['INPUT','TEXTAREA','SELECT'].includes(document.activeElement?.tagName);
 if((e.key==='Delete'||e.key==='Backspace')&&selected&&!inField){if(selected===lastEditable)lastEditable=null;selected.remove();clearSelection()}
 if(e.key==='Escape'){if(document.activeElement?.id==='liveTextEditor'){e.preventDefault();document.activeElement.blur();return} if(pasteDraft){cancelPasteMode();setStatus('Вставка отменена');return} dimDraft=null;textDraft=null;dimPreview?.remove();textPreview?.remove();dimPreview=textPreview=null;activeImage=null;clearSelection();setTool(null,true)}
},true);
$('dimPopupClose').onclick=closeDimPopup;$('dimPopupOk').onclick=()=>{if(selected?.dataset.type==='dimension')captureDimDefaults(selected);closeDimPopup();clearSelection();setTool(null,true);setStatus('Размер зафиксирован')};$('dimSetDefault').onclick=()=>{if(selected?.dataset.type==='dimension'){captureDimDefaults(selected);setStatus('Настройки размера сохранены по умолчанию')}};
$('textPopupClose').onclick=closeTextPopup;$('textOk').onclick=()=>{document.getElementById('liveTextEditor')?.blur();closeTextPopup();clearSelection();setTool(null,true);setStatus('Надпись зафиксирована')};
$('dimPopup').addEventListener('pointerdown',e=>e.stopPropagation());$('textPopup').addEventListener('pointerdown',e=>e.stopPropagation());$('dimPopupHeader').addEventListener('pointerdown',e=>{if(e.target.id==='dimPopupClose')return;const r=$('dimPopup').getBoundingClientRect();popupDrag={dx:e.clientX-r.left,dy:e.clientY-r.top};e.preventDefault()});$('textPopupHeader').addEventListener('pointerdown',e=>{if(e.target.id==='textPopupClose')return;const r=$('textPopup').getBoundingClientRect();textPopupDrag={dx:e.clientX-r.left,dy:e.clientY-r.top};e.preventDefault()});window.addEventListener('pointermove',e=>{if(popupDrag){const p=$('dimPopup');p.style.left=Math.max(0,Math.min(innerWidth-p.offsetWidth,e.clientX-popupDrag.dx))+'px';p.style.top=Math.max(0,Math.min(innerHeight-p.offsetHeight,e.clientY-popupDrag.dy))+'px'} if(textPopupDrag){const p=$('textPopup');p.style.left=Math.max(0,Math.min(innerWidth-p.offsetWidth,e.clientX-textPopupDrag.dx))+'px';p.style.top=Math.max(0,Math.min(innerHeight-p.offsetHeight,e.clientY-textPopupDrag.dy))+'px'}});window.addEventListener('pointerup',()=>{popupDrag=null;textPopupDrag=null});
